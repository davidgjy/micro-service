server:
  port: 8300
spring:
  application:
    name: origin-consumer-ribbon
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
